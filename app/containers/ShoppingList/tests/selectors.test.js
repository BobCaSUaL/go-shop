// import { selectShoppingListDomain } from '../selectors';

describe('selectShoppingListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
